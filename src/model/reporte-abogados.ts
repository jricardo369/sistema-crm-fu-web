// Modelo base para reporte de abogados (puedes expandirlo según los datos necesarios)
export interface ReporteAbogados {
  id: number;
  nombre: string;
  email: string;
  telefono: string;
  // agrega más campos según sea necesario
}
